package secondbrain.domain.config;

/**
 * Represents the option to skip empty results when filtering based on ratings in the last duration.
 */
public interface LocalSkipEmptyInLastDuration {
    boolean isSkipEmptyInLastDuration();
}
